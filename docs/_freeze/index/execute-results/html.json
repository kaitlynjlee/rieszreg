{
  "hash": "970d499a79cc2aa01c62c87c9b7d8abe",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"RieszReg\"\ntitle-block-style: none\nengine: knitr\n---\n\n\n\n\n\n\n![](banner.svg){.hero-banner fig-alt=\"RieszReg — Riesz regression for semiparametric inference\"}\n\n`rieszreg` estimates the Riesz representer $\\alpha$ of a linear estimand $\\psi = \\mathbb{E}[m(\\mu)(Z,Y)]$ — the building block of one-step, TMLE, and DML inference — with gradient boosting ([`rieszboost`](backends/boosting.qmd)), kernel ridge ([`krrr`](backends/kernel.qmd)), random forests ([`forestriesz`](backends/forest.qmd)), a single decision tree ([`riesztree`](backends/tree.qmd)), or neural nets ([`riesznet`](backends/neural.qmd)). Think of it as the `sklearn`, `tidymodels` or `superlearner` for Riesz regression.\n\n`rieszreg` ships built-in factories for ATE, ATT, TSM, additive shifts, local shifts, stochastic interventions, and arbitrary user-defined estimands; the squared Riesz loss plus the wider Bregman family (KL, Bernoulli, bounded squared); and sklearn-native tuning, model selection, and cross-fitting. Python and R, with bitwise-identical predictions on the same input.\n\n## What is Riesz regression?\n\nYou want to estimate some causal or structural estimand — say, the average treatment effect $\\psi = \\mathbb{E}[\\mu(1, X) - \\mu(0, X)]$, where $\\mu(a, x) = \\mathbb{E}[Y \\mid A = a, X = x]$. A regression $\\hat\\mu$ for $\\mu$ is biased; the standard fix uses a weight. For ATE, the weight is the inverse-propensity score:\n\n$$\n\\alpha(a, x) \\;=\\; \\frac{a}{\\pi(x)} - \\frac{1-a}{1-\\pi(x)},\n$$\n\nUnder the [Riesz representation theorem](intro.qmd), similar weights exist for many different estimands. `rieszreg` learns these weights directly from data: no propensity model, no truncation, etc. The same machinery handles continuous shifts, stochastic interventions, and many custom estimands a user can write down — for which closed-form weights could otherwise involve tricky or slow density estimates. You can then plug these weights into weighting, one-step, DML, or TMLE estimators.\n\nFor the longer derivation and a survey of the closed-form weights for each built-in estimand, see [What is Riesz regression?](intro.qmd). \n\n## Quickstart\n\nFit a Riesz representer for ATE on a synthetic binary-treatment dataset, select between two backends with cross-validation, and use the cross-fit $\\hat\\alpha$ for a DML point estimate and Wald CI.\n\n::: {.panel-tabset group=\"lang\"}\n\n## Python\n\n\n\n::: {.cell}\n\n```{.python .cell-code  code-fold=\"true\" code-summary=\"Simulate\"}\nimport os\nos.environ.update({\"OMP_NUM_THREADS\": \"1\", \"MKL_NUM_THREADS\": \"1\"})\nimport numpy as np\nimport pandas as pd\n\nrng = np.random.default_rng(0)\nn = 2000\nx = rng.uniform(0, 1, n)\npi = 1 / (1 + np.exp(-(-0.02 * x - x**2 + 4 * np.log(x + 0.3) + 1.5)))\na = rng.binomial(1, pi)\nmu = 5 * x + 9 * x * a + 5 * np.sin(x * np.pi) + 25 * (a - 2)\ny = mu + rng.normal(0, 1.0, n)\ndf = pd.DataFrame({\"a\": a.astype(float), \"x\": x, \"y\": y})\ntrue_alpha = a / pi - (1 - a) / (1 - pi)\n```\n:::\n\n\n\nWe start with a data frame `df` with treatment `a`, covariate `x`, outcome `y`. In this case, since this is simulated data, we know the true Riesz representer $\\alpha_0$ to evaluate against later.\n\n\n\n::: {.cell}\n\n```{.python .cell-code}\nimport torch\ntorch.set_num_threads(1)\nfrom rieszreg import ATE, BoundedSquaredLoss\nfrom rieszboost import RieszBooster\nfrom riesznet import RieszNet\n\nestimand = ATE(treatment=\"a\", covariates=(\"x\",))\nloss     = BoundedSquaredLoss(lo=-50.0, hi=50.0)\n\nboost = RieszBooster(\n    estimand=estimand, loss=loss,\n    n_estimators=200, learning_rate=0.05, max_depth=3,\n)\nnet   = RieszNet(\n    estimand=estimand, loss=loss,\n    hidden_sizes=(32, 32), epochs=60, learning_rate=1e-2,\n)\n```\n:::\n\n\n\nWrap the candidates in a `Pipeline` and let `GridSearchCV` pick the winner via inner CV; nest that selector inside `cross_val_predict` so each outer fold runs its own inner model selection on the training portion only. The result `alpha_hat` is out-of-fold — every row was predicted by a model fit without seeing it.\n\n\n\n::: {.cell}\n\n```{.python .cell-code}\nfrom sklearn.model_selection import GridSearchCV, KFold, cross_val_predict\nfrom sklearn.pipeline import Pipeline\n\ncv = KFold(n_splits=5, shuffle=True, random_state=0)\nselector = GridSearchCV(\n    Pipeline([(\"riesz\", boost)]),\n    param_grid=[{\"riesz\": [boost, net]}],\n    cv=3, refit=True,\n)\nalpha_hat = cross_val_predict(selector, df, cv=cv)\n```\n:::\n\n\n\n### Sanity check\n\n\n\n::: {.cell}\n::: {.cell-output-display}\n![Out-of-fold $\\hat\\alpha$ vs the closed-form $\\alpha_0$ on the synthetic ATE DGP.](index_files/figure-html/unnamed-chunk-5-1.png){width=672}\n:::\n:::\n\n\n\n### DML point estimate and CI\n\nCross-fit the outcome regression $\\hat\\mu$ on the same folds. In each fold, fit on the training rows and predict the held-out rows at their observed treatment and at $A = 1$ and $A = 0$:\n\n\n\n::: {.cell}\n\n```{.python .cell-code  code-fold=\"true\" code-summary=\"Cross-fit outcome regression\"}\nfrom sklearn.base import clone\nfrom sklearn.ensemble import GradientBoostingRegressor\n\nmu_model = GradientBoostingRegressor(\n    n_estimators=300, learning_rate=0.05, max_depth=3, random_state=0,\n)\nZ_obs = df[[\"a\", \"x\"]]\nyv = df[\"y\"].to_numpy()\nmu_obs, mu1, mu0 = np.zeros(n), np.zeros(n), np.zeros(n)\nfor train, test in cv.split(df):\n    fit_k = clone(mu_model).fit(Z_obs.iloc[train], yv[train])\n    mu_obs[test] = fit_k.predict(Z_obs.iloc[test])\n    mu1[test]    = fit_k.predict(Z_obs.iloc[test].assign(a=1.0))\n    mu0[test]    = fit_k.predict(Z_obs.iloc[test].assign(a=0.0))\n```\n:::\n\n::: {.cell}\n\n```{.python .cell-code}\neif  = (mu1 - mu0) + alpha_hat * (yv - mu_obs)\npsi  = float(eif.mean())\nse   = float(np.sqrt(np.var(eif, ddof=1) / n))\nci   = (psi - 1.96 * se, psi + 1.96 * se)\nprint(f\"ψ̂_ATE  = {psi:.3f}\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nψ̂_ATE  = 29.465\n```\n\n\n:::\n\n```{.python .cell-code}\nprint(f\"SE     = {se:.3f}\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSE     = 0.095\n```\n\n\n:::\n\n```{.python .cell-code}\nprint(f\"95% CI = [{ci[0]:.3f}, {ci[1]:.3f}]\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n95% CI = [29.279, 29.650]\n```\n\n\n:::\n:::\n\n\n\n## R\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"Load riesznet R wrapper\"}\n.project_root <- Sys.getenv(\"QUARTO_PROJECT_DIR\", unset = getwd())\nsuppressMessages(pkgload::load_all(\n  file.path(.project_root, \"../packages/riesznet/r/riesznet\"), quiet = TRUE))\n```\n:::\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"Simulate\"}\nset.seed(0)\nn     <- 2000\nx     <- runif(n)\nprop  <- 1 / (1 + exp(-(-0.02 * x - x^2 + 4 * log(x + 0.3) + 1.5)))\na     <- rbinom(n, 1, prop)\nmu_dgp <- 5 * x + 9 * x * a + 5 * sin(x * pi) + 25 * (a - 2)\ny     <- mu_dgp + rnorm(n, 0, 1)\ndf    <- data.frame(a = as.numeric(a), x = x, y = y)\ntrue_alpha <- a / prop - (1 - a) / (1 - prop)\n```\n:::\n\n::: {.cell}\n\n```{.r .cell-code}\nestimand <- ATE(treatment = \"a\", covariates = \"x\")\nloss     <- BoundedSquaredLoss(lo = -50, hi = 50)\nmake_boost <- function() RieszBooster$new(\n  estimand = estimand, loss = loss,\n  n_estimators = 200L, learning_rate = 0.05, max_depth = 3L\n)\nmake_net <- function() RieszNet$new(\n  estimand = estimand, loss = loss,\n  hidden_sizes = c(32L, 32L), epochs = 60L, learning_rate = 0.01\n)\n```\n:::\n\n\n\nUse [`rsample`](https://rsample.tidymodels.org/) (tidymodels) for fold splits, and a small loop for the nested cross-fit + per-fold model selection. Each outer fold runs its own inner-CV winner pick on its training portion only.\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsuppressPackageStartupMessages(library(rsample))\n\ncv_outer <- vfold_cv(df, v = 5)\nalpha_hat <- numeric(nrow(df))\nfor (split in cv_outer$splits) {\n  tr <- analysis(split); te <- assessment(split); te_idx <- complement(split)\n  cv_inner <- vfold_cv(tr, v = 3)\n  inner_score <- function(make) mean(vapply(cv_inner$splits, function(s) {\n    m <- make(); m$fit(analysis(s)); m$score(assessment(s))\n  }, numeric(1)))\n  best_make <- if (inner_score(make_boost) >= inner_score(make_net)) make_boost else make_net\n  best <- best_make(); best$fit(tr)\n  alpha_hat[te_idx] <- best$predict(te)\n}\n```\n:::\n\n\n\nSanity check $\\hat\\alpha$ against the closed-form $\\alpha_0$:\n\n\n\n::: {.cell}\n::: {.cell-output-display}\n![Out-of-fold $\\hat\\alpha$ vs the closed-form $\\alpha_0$ (R).](index_files/figure-html/unnamed-chunk-12-1.png){width=672}\n:::\n:::\n\n\n\nCross-fit outcome regression $\\hat\\mu$ via xgboost on the same folds, predicting each held-out fold at its observed treatment and at $A = 1$ and $A = 0$. Then assemble the DML estimate and Wald CI:\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"Cross-fit outcome regression\"}\nsuppressPackageStartupMessages(library(xgboost))\n\nxgb_params <- list(objective = \"reg:squarederror\", eta = 0.05,\n                   max_depth = 3, lambda = 1, verbosity = 0)\nmu_obs <- mu1 <- mu0 <- numeric(nrow(df))\nfor (split in cv_outer$splits) {\n  tr <- analysis(split); te <- assessment(split); te_idx <- complement(split)\n  bst <- xgb.train(\n    params = xgb_params,\n    data = xgb.DMatrix(as.matrix(tr[, c(\"a\", \"x\")]), label = tr$y),\n    nrounds = 300\n  )\n  X_te <- as.matrix(te[, c(\"a\", \"x\")])\n  mu_obs[te_idx] <- predict(bst, X_te)\n  mu1[te_idx]    <- predict(bst, cbind(a = 1, X_te[, \"x\", drop = FALSE]))\n  mu0[te_idx]    <- predict(bst, cbind(a = 0, X_te[, \"x\", drop = FALSE]))\n}\n```\n:::\n\n::: {.cell}\n\n```{.r .cell-code}\neif <- (mu1 - mu0) + alpha_hat * (df$y - mu_obs)\npsi <- mean(eif)\nse  <- sqrt(var(eif) / length(eif))\nci  <- c(psi - 1.96 * se, psi + 1.96 * se)\ncat(sprintf(\"psi_ATE = %.3f\\nSE      = %.3f\\n95%% CI  = [%.3f, %.3f]\\n\",\n            psi, se, ci[1], ci[2]))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\npsi_ATE = 29.508\nSE      = 0.088\n95% CI  = [29.335, 29.681]\n```\n\n\n:::\n:::\n\n\n\n:::\n\nFor TMLE on top of these nuisances, and integration with `DoubleML` and other downstream packages, see [Estimation](estimation/index.qmd). For backend-specific tuning, see [Backends](backends/index.qmd).\n\n::: {.callout-tip title=\"Code execution\"}\nEvery code block on this page ran at build time. The numbers and figures came from the version of `rieszreg`, `rieszboost`, `krrr`, `forestriesz`, `riesztree`, and `riesznet` that produced this page.\n:::\n",
    "supporting": [
      "index_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}